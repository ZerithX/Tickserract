{
  "name": "tickserract",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "db:push": "bun --filter \"be\" db:push",
    "db:generate": "bun --filter \"be\" db:generate",
    "db:studio": "bun --filter \"be\" db:studio"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.12.18"
  }
}